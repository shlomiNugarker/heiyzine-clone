// Application Constants - All magic numbers and configuration values

// PDF Rendering
export const PDF_RENDER_SCALE = 2.5; // Scale factor for PDF rendering quality (higher = better quality)

// Sidebar
export const SIDEBAR_WIDTH_OPEN = 280; // Sidebar width when open (pixels)
export const SIDEBAR_WIDTH_COLLAPSED = 48; // Sidebar width when collapsed (pixels)

// Supported Languages
export const SUPPORTED_LOCALES = ['en', 'he', 'es', 'fr', 'de', 'ja', 'zh'] as const;
